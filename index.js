/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import StacksPage from "./StacksPage";


AppRegistry.registerComponent(appName, () => StacksPage);
